package com.company;

import java.util.Arrays;
import java.util.List;
import java.util.Random;
import java.util.stream.Collectors;
import java.util.stream.Stream;

public class Main {

    public static void main(String[] args) {
        Program p = new Program();







//        // The data to use in the stream
//        String[] myArray = new String[]{"a1", "a2", "b1", "c2", "c1"};
//        List<String> myList = Arrays.asList("a1", "a2", "b1", "c2", "c1");
//
//        // Create a stream in any of the following alternatives
//        Stream<String> myStream = Stream.of("a1", "a2", "b1", "c2", "c1");
//        Stream<String> listStream = myList.stream();
//        Stream<String> streamOfArray = Stream.of(myArray);
//        Stream<String> arraysStream = Arrays.stream(myArray);
//
////        var shortTest = Stream.of("hi", "hello", "hey", "greetings", "aloha")
////                .collect(Collectors.toList());
////        shortTest.forEach(System.out::println);
//
//
//        var differentGreetings = Stream.of("hi", "hello", "hey", "greetings", "aloha")
//                .parallel()
//                .peek(s -> System.out.println("Original: " + s))
//                .filter(s -> s.length() > 3)
//                .peek(s -> System.out.println("After filter: " + s))
//                .map(String::toUpperCase)
//                .peek(s -> System.out.println("After map: " + s))
//                .sorted((a, b) -> b.length() - a.length())
//                .peek(s -> System.out.println("Final: " + s))
//                .collect(Collectors.toList());
////
//        //differentGreetings.forEach(System.out::println);
//
    }
}
